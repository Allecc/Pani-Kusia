extends layout
block content
  .parallax.parallax-single-product(ng-controller='singleProductController')
    .parallax-row
      .textContainers
        h3.text-center O Sklepie
        p=product.title

    script.
      angular
        .module('singleProduct', [])
        .controller('singleProductController', function($scope, $http){
          console.log('balblalalal');
          $http.get(window.location.href)
            .then(function(data){
              console.log(data);
            });
          
          console.log('Something ' + product);
          $scope.product = product;
        });
